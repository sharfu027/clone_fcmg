# PostgreSQL Stored Functions & Expressions

This directory holds PostgreSQL PL/pgSQL functions for domain logic extensions.

## Usage
- Immutable calculation functions (GST tax computations, FEFO allocations, aging buckets).
